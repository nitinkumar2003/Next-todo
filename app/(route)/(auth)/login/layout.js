
export const metadata = {
    title: "Login",
    description: "Login Description"
}
export default function LoginLayout({ children }) {
  return (
    <html lang="en">
      <body  >
        {children}
      </body>
    </html>
  );
}
